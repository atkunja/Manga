# Web backend
fastapi
uvicorn[standard]

# Video/image processing
moviepy>=2.0.0
pillow

# Frontend
streamlit

# HTTP requests for Streamlit frontend
requests

# Optional: for file uploads/extraction (already built into Python)
# zipfile is built-in, no need to install

# If you use typing-extensions for newer type hints, add:
typing-extensions

# If you're deploying, you might want python-multipart for file uploads:
python-multipart
